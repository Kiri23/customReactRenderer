# Custom React Renderer Rules

## Core File Management Rules
- ALWAYS keep both index.js and index.jsx files
- index.js should contain the original React.createElement version
- index.jsx should contain the JSX version
- NEVER delete either file
- Both files should be functional and working

## User Interaction Guidelines
Do not remove any comment made from the user

Cuando te menciones que quiero hacer brainstomrm o vamos a pensar esta idea o solamente necesite redirecion, piensa un momento analiza lo que el usuario te esta pidiendo y contestale sin ir directo a la solucion, piensa en el problema y la solucion. Para eso no tienes que escribir o cambiar el codigo, pq solamente estamos reflexionando cual es la mejor manera de resolver el problema.

Cuando te menciones que quiero hacer brainstomrm no solo apruebes mi idea, piensa en la mejor manera de resolver el problema, y critica mi idea.

## JSX Expressiveness Rules
- NEVER forget the expressiveness and readability of JSX
- JSX provides clear, hierarchical structure that is easy to understand
- Tagged Templates should create JSX, NOT generate LaTeX directly
- The goal is to write LaTeX using JSX syntax for better readability
- Maintain the JSX → LaTeX transformation pipeline for expressiveness
- JSX allows for nested structures, props, and dynamic content with composing components in a readable way

## Testing Rules
- ALWAYS write tests when adding new features to ensure nothing breaks
- Run existing tests after any code changes to verify functionality
- Create snapshot tests for new components to ensure consistent output
- Test edge cases and error scenarios for robust functionality

## Development Standards
Use JSX whenever possible, but keep the original React.createElement version for the index.js file. Use uppercase first letter for the components name.

EVITA EL USO DE REACT CREACTE ELEMENT PARA CREAR COMPONENTES, USAR TAGGED TEMPLATES.

## Tagged Templates Guidelines
el script test-tikz-simple.js es un ejemplo de como se puede usar tagged templates para crear componentes TikZ.

Se puede correr desde pacakge.json usando el comando yarn example:tikz-simple el cual transpila el JSX a JS y lo ejecuta.

## Package Manager
USA YARN AS YOUR PACKAGE MANAGER.



